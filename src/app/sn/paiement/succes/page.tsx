import type { Metadata } from "next";
import PaymentSuccessPage from "@/components/pages/PaymentSuccessPage";

export const metadata: Metadata = {
  title: "Paiement réussi · SafePhone",
};

export default PaymentSuccessPage;
