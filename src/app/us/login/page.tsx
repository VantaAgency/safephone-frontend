import type { Metadata } from "next";
import LoginPage from "@/components/pages/LoginPage";

export const metadata: Metadata = {
  title: "Log in · SafePhone",
  description: "Sign in to your SafePhone account.",
};

export default LoginPage;
