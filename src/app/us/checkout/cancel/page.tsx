import type { Metadata } from "next";
import USCheckoutCancelPage from "@/components/pages/USCheckoutCancelPage";

export const metadata: Metadata = {
  title: "Checkout cancelled · SafePhone",
  description:
    "Your SafePhone checkout was cancelled. Pick a plan again whenever you're ready.",
};

export default USCheckoutCancelPage;
