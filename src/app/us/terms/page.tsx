import type { Metadata } from "next";
import USTermsPage from "@/components/pages/USTermsPage";

export const metadata: Metadata = {
  title: "Terms of service · SafePhone",
  description:
    "The terms that apply when you use SafePhone's website and phone repair protection membership.",
};

export default USTermsPage;
