import type { Metadata } from "next";
import USRegisterDevicePage from "@/components/pages/USRegisterDevicePage";

export const metadata: Metadata = {
  title: "Register your phone · SafePhone",
  description:
    "Tell us which phone is covered by your SafePhone membership so claims are ready when you need them.",
};

export default USRegisterDevicePage;
